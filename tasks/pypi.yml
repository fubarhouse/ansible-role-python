---
# file: roles/fubarhouse.python/tasks/pypi.yml

- name: "PyPI | Ensure installed and updated"
  become: yes
  become_user: "{{ fubarhouse_user }}"
  pip:
    name: "{{ item }}"
    state: latest
    executable: "{{ main_dir }}/versions/{{ python_version }}/bin/pip"
  with_items: "{{ python_packages }}"
