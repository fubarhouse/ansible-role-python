---

- hosts: all
  sudo: true
  vars:
    ansible_ssh_user: root
    python_version: 2.7
    python_versions:
      - 2.7
    python_packages:
      - virtualenv
  roles:
    - { role: ../../fubarhouse.python }
